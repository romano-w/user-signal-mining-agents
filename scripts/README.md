Planned script entrypoints:

- `build_yelp_subset.py`: filter Yelp data down to a restaurant-focused working subset
- `build_index.py`: generate embeddings and persist the retrieval index
- `run_experiment.py`: run baseline and pipeline variants over the founder prompt set
